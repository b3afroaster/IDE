import numpy as np
def random_pridict(number: int=1) -> int:
    """bombastic function that guess number

    Args:
        number (int, optional): Загаданное число. Defaults to 1.

    Returns:
        int: Number of tries
    """